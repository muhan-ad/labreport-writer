# 实验报告自动编写应用 · 整体方案设计

> 版本：v1.0 ｜ 日期：2026-09-06
> 定位：在《实验报告自动编写应用-方案设计.md》（产品方案 v1.0）基础上的**技术方案细化**，输出可直接指导开发的模块边界、接口、协议与数据模型。
> 参考：《开发参考资料.md》（dsh 架构借鉴：模块接口化 / 守卫式工具流水线 / 事件化 / 配置分层 / 双驱动）。

---

## 一、设计范围与基线

| 项 | 基线 |
|---|---|
| 形态 | Electron 桌面应用（Windows 优先），本地、免费、数据自持 |
| 前端 | Electron + electron-vite + React 18 + TypeScript |
| Agent | Python 3.10+ LangGraph，stdio JSON-RPC 子进程通信 |
| 计算 | 复用 物理实验.zip 的 `common/` 库（uncertainty / regression / latex_formatter） |
| 绘图 | matplotlib（Python 侧，chart.json 驱动，确定性输出） |
| 公式 | KaTeX 语法书写 → xelatex 编译（PDF + .tex） |
| OCR | RapidOCR（PP-OCRv5，本地离线） |
| 复用骨架 | windows-app-maker（main/preload/renderer 三段式、stdio 通信模式、safeStorage 加密、权限门） |

**设计原则（与产品方案一致，落为机制）**：
1. 数值真实性 → 计算/绘图为**受守卫的确定性工具**，Agent 只写文字（守卫流水线，见 §4.2）；
2. 公式由 AI 写 → Agent 依据素材库事实动态生成 KaTeX，离线用素材库兜底（双驱动，见 §7.4）；
3. 模板最大自主权 → 正文 100% 来自模板，Agent 只做细微调整；
4. 图表确定性 → 图表规格由 chart.json 声明，Agent 不自由发挥；
5. 编译兜底 → 失败读 .log 自动修复 ≤3 次，仍失败给错误定位与建议；
6. 个人信息 → 全局设置层，不进模板不进表单（配置分层，见 §5.4）；
7. 模型独立配置 → 本应用独立一套 LLM 配置。

---

## 二、总体架构

```
┌────────────────────────────────────────────────────────────┐
│ Electron 主进程（Node/TS）                                  │
│  ├─ main/index.ts      应用生命周期、窗口、子进程管理       │
│  ├─ main/agent-host.ts Python 子进程启动/心跳/RPC 客户端    │
│  ├─ main/template-mgr.ts 模板库管理（导入/校验/索引）       │
│  ├─ main/compile-svc.ts 编译任务调度（调 Python compiler）  │
│  ├─ main/settings-svc.ts settings.json 读写（safeStorage） │
│  ├─ preload/            contextBridge 暴露受限 API          │
│  └─ renderer/           React 页面（模板库/表单/源码/设置） │
└──────────────┬─────────────────────────────────────────────┘
               │ stdio JSON-RPC 2.0（单子进程，事件广播）
┌──────────────▼─────────────────────────────────────────────┐
│ Python Agent 子进程（agent/，LangGraph 编排）               │
│  ├─ main.py            stdio 分发器 + 方法路由 + 事件广播    │
│  ├─ guard.py           ★ 守卫流水线（pre/execute/post）     │
│  ├─ template_parser.py 三文件 → 表单 schema                 │
│  ├─ compute.py         IComputeEngine（复用 common/）       │
│  ├─ plot.py            IPlotRenderer（matplotlib）          │
│  ├─ compose.py         IComposer（在线 Agent / 离线模板）    │
│  ├─ compiler.py        ICompiler（xelatex + 日志修复）       │
│  ├─ ocr_bridge.py      IOcrEngine（RapidOCR）               │
│  └─ graph.py           LangGraph 工作流定义                 │
└─────────────────────────────────────────────────────────────┘
        本地数据：templates/ · assets/ · outputs/ · settings.json
```

**关键决策**：
- Python 子进程**常驻**（懒启动 + 空闲回收），避免每次生成都重新加载 common/ 库与 OCR 模型；
- 一切耗时操作（计算/绘图/OCR/编译）走 RPC，前端不阻塞；进度通过事件广播；
- 模板解析、计算、绘图、编译全部在 Python 侧完成，Electron 侧只做 UI 与文件管理——保证"计算引擎唯一实现"，杜绝双份逻辑。

---

## 三、进程模型与通信协议

### 3.1 子进程生命周期

```
主进程启动 → 探测 Python（settings.python_path）→ spawn pythonw -m agent.main
  → 握手 initialize（返回版本/能力清单）
  → 空闲 5 分钟自动退出（可配置）；主进程退出时发送 shutdown
  → 崩溃：主进程捕获退出码，提示用户并允许一键重启
```

### 3.2 JSON-RPC 2.0 消息格式

```
请求   {"jsonrpc":"2.0","id":1,"method":"report.generate","params":{...}}
响应   {"jsonrpc":"2.0","id":1,"result":{...}}
错误   {"jsonrpc":"2.0","id":1,"error":{"code":-32602,"message":"...","data":{...}}}
事件   {"jsonrpc":"2.0","method":"event","params":{"type":"progress","data":{...}}}
```

### 3.3 方法清单（v1 范围）

| 方法 | 方向 | 说明 |
|---|---|---|
| `initialize` | C→S | 握手，返回版本、能力、环境检查（xelatex/OCR 可用性） |
| `template.parse` | C→S | 解析模板三文件 → 表单 schema |
| `compute.calc` | C→S | 批量计算计算列（受守卫） |
| `plot.render` | C→S | 按 chart.json 生成图表文件（受守卫） |
| `ocr.recognize` | C→S | 图片 → 结构化数据表（含置信度） |
| `agent.compose` | C→S | 组稿：返回 tex 片段（在线 Agent / 离线模板替换） |
| `compile.run` | C→S | xelatex 编译 + 自动修复，返回 PDF 路径/日志 |
| `report.generate` | C→S | 一条龙流水线（表单+数据 → 完整报告），事件广播进度 |
| `report.list` / `report.read` | C→S | 历史报告查询 |
| `shutdown` | C→S | 优雅退出 |

### 3.4 事件类型

| 事件 | 载荷 | 触发点 |
|---|---|---|
| `progress` | `{stage, percent, message}` | 流水线各阶段 |
| `log` | `{level, message}` | 子进程内部日志（debug 用） |
| `compile.retry` | `{attempt, error, fix}` | 编译修复重试 |
| `agent.status` | `{mode: online\|offline}` | 组稿模式切换 |

### 3.5 错误码规范

| 码 | 含义 |
|---|---|
| `-32600` | 请求无效 |
| `-32602` | 参数校验失败（附 data.schema_errors） |
| `1001` | 模板解析失败 |
| `1002` | 数据校验失败（NaN/Inf/越界） |
| `1003` | 计算失败（算法异常） |
| `1004` | 绘图失败 |
| `1005` | OCR 失败 |
| `1006` | 编译失败（含 .log 摘要与修复建议） |
| `1007` | LLM 不可用（触发离线降级信号） |

---

## 四、Python 侧模块与接口设计

### 4.1 接口定义（Seam 三件套：接口 / 实现 / 消费方）

```python
# agent/interfaces.py —— 全部接口先定义，实现可替换
from typing import Any, Protocol, runtime_checkable

@runtime_checkable
class IComputeEngine(Protocol):
    """计算列引擎：唯一允许产生数值的实现。"""
    def calc(self, spec: dict, data: dict[str, list]) -> dict[str, list]: ...

@runtime_checkable
class IPlotRenderer(Protocol):
    """图表渲染：完全由 chart.json 驱动。"""
    def render(self, spec: dict, data: dict) -> list[dict]: ...  # 返回 [{figure_path, name}]

@runtime_checkable
class IComposer(Protocol):
    """组稿：把表单+计算结果+图表 → report.tex 正文片段。"""
    def compose(self, form: dict, computed: dict, plots: list[dict],
                mode: str) -> dict: ...  # {tex, warnings}

@runtime_checkable
class ICompiler(Protocol):
    """LaTeX 编译 + 失败自动修复。"""
    def compile(self, tex_path: str, max_retry: int = 3) -> dict: ...
    # {pdf_path, status: ok|failed, log_summary, fixes: [...]}

@runtime_checkable
class IOcrEngine(Protocol):
    """OCR 识别：图片 → 行列对齐数据。"""
    def recognize(self, image_path: str, hints: dict | None = None) -> dict: ...
    # {table: {columns, rows}, confidence, layout}
```

实现类：`CommonComputeEngine`（import common/）、`MatplotlibPlotter`、`AgentComposer` / `TemplateComposer`、`XelatexCompiler`、`RapidOcrEngine`。

### 4.2 守卫流水线（对应"数值真实性"原则）

```python
# agent/guard.py —— 所有数值/文件产出必须经过守卫
def guarded(engine_fn):
    def wrapper(spec, data, **kw):
        pre_validate(spec, data)          # 白名单：calc 类型枚举、round∈[0,9]、列存在性
        result = engine_fn(spec, data)    # 执行（确定性代码，非 LLM）
        post_validate(result)             # NaN/Inf 检查、数值范围、文件存在性
        return result
    return wrapper
```

**守卫规则（写死，不允许 LLM 参与）**：
- `compute.calc`：type 必须 ∈ {mean,std,ua,ub,uc,slope,intercept,r}；round ∈ [0,9]；引用的列必须存在于数据表；结果不允许 NaN/Inf；
- `plot.render`：图表参数必须来自 chart.json（agent 不可改）；输出文件必须真实存在且非空；
- `agent.compose`：**只允许返回文字与公式片段**（类型检查 + 长度上限），不允许返回数值——所有数值只能来自计算列。

### 4.3 agent/main.py 分发器

```
启动 → 读 stdin 行（JSON）→ 路由到对应 handler → 写 stdout（JSON）
handler 统一签名：handler(params, ctx) -> result | raise AgentError(code, msg)
ctx 提供：settings、模板缓存、事件广播（emit(event_type, data)）
```

### 4.4 计算引擎（复用 common/）

| 计算列 type | 实现 | 输入 |
|---|---|---|
| `mean` | 算术平均 | `col` |
| `std` | 标准差（贝塞尔，样本标准差） | `col` |
| `ua` | A 类不确定度 s/√n | `col` |
| `ub` | B 类不确定度 仪器精度/√3 | `col, instrument_precision` |
| `uc` | 合成不确定度 √(ua²+ub²)（或由公式传递） | `ua, ub` 或 `formula` |
| `slope` / `intercept` / `r` | 线性回归（common/regression.py） | `x_col, y_col` |

数值→LaTeX 格式化复用 `common/latex_formatter.py`（有效数字对齐、± 形式）。

---

## 五、数据模型

### 5.1 表单 schema（template.parse 产物）

```json
{
  "experiment": "伏安法测电阻",
  "fields": [{"key": "仪器", "label": "实验仪器", "type": "text", "required": true}],
  "tables": [{
    "name": "数据1",
    "columns": [{"key": "I/mA", "label": "I/mA", "type": "number"},
                {"key": "U/V", "label": "U/V", "type": "number"}],
    "rows": 5,
    "calc_columns": [
      {"key": "R/Ω", "type": "slope", "x": "I/mA", "y": "U/V", "round": 3}
    ]
  }],
  "charts": [{"name": "U-I曲线", "type": "scatter", "x": "I/mA", "y": "U/V",
              "fit": "linear", "error_bar": "y", "title": "U-I 特性曲线"}],
  "sections": ["原理", "步骤", "数据处理", "误差分析", "结论"]
}
```

### 5.2 chart.json schema（模板声明，v1）

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["图表"],
  "properties": {
    "图表": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "type", "x", "y"],
        "properties": {
          "name":    {"type": "string"},
          "type":    {"enum": ["scatter", "line", "bar"]},
          "x":       {"type": "string"},
          "y":       {"type": "string"},
          "fit":     {"enum": ["linear", "poly2", "none"], "default": "none"},
          "error_bar": {"enum": ["x", "y", "none"], "default": "none"},
          "title":   {"type": "string"},
          "xlabel":  {"type": "string"},
          "ylabel":  {"type": "string"},
          "legend":  {"type": "boolean", "default": true}
        }
      }
    }
  }
}
```

### 5.3 settings.json（全局配置层）

```json
{
  "personal":   {"姓名": "", "学号": "", "学校": "", "班级": ""},
  "llm":        {"provider": "openai-compatible", "base_url": "",
                 "api_key_encrypted": "", "model": "", "temperature": 0.2},
  "compile":    {"engine": "xelatex", "xelatex_path": "", "passes": 2, "max_retry": 3},
  "ocr":        {"enabled": true, "min_confidence": 0.85},
  "agent":      {"mode": "auto", "timeout_sec": 60},
  "paths":      {"templates": "templates", "assets": "assets", "outputs": "outputs"}
}
```

配置分层（借鉴 dsh profile+patch）：**全局层（settings.json）→ 模板层（templates/<实验名>/chart.json 等）→ 会话层（本次表单输入）**，逐层覆盖，互不污染。

### 5.4 目录结构（定稿）

```
labreport-writer/
├─ src/main/  src/preload/  src/renderer/     # Electron + React
├─ agent/     # Python（interfaces/guard/parser/compute/plot/compose/compiler/ocr/graph/main）
├─ templates/ # 用户导入（每实验 3 文件）
├─ assets/    # 内置素材库（21 实验：公式事实/思考题/图表规格 JSON）
├─ outputs/   # PDF + .tex + figures/ + history/（事件日志 + 产物）
├─ settings.json
└─ package.json / pyproject.toml
```

### 5.5 历史报告（事件化，借鉴 dsh 会话日志）

`outputs/history/<报告ID>/` 下保存：
- `events.jsonl`（append-only：表单输入→计算参数与结果→绘图参数→组稿片段→编译日志，逐条记录，"模型可见即已记录"）；
- `report.tex` / `report.pdf` / `figures/`；
- `meta.json`（实验名、时间、模式 online/offline、编译状态）。

可回看、可审计、可复现；重放 = 依次应用 events 到空状态。

---

## 六、核心流水线与状态机

### 6.1 状态机

```
TEMPLATE_PARSED → DATA_FILLED → COMPUTED → PLOTTED → COMPOSED → COMPILED ✓
        │              │           │          │          │
        └────── 任一失败 → FAILED（记录 error，支持从该状态续跑）
COMPILED 失败路径：COMPILING → (日志解析+修复) → 重试 ≤3 次 → COMPILED | FAILED
```

### 6.2 report.generate 流水线（Python 侧一次性执行，事件广播）

```
① 解析模板（template_parser）→ emit progress(10%)
② 校验表单数据（列存在/数值合法/行数一致）→ emit progress(25%)
③ compute.calc 批量计算（守卫）→ emit progress(40%)
④ plot.render（守卫：仅按 chart.json）→ emit progress(55%)
⑤ compose：在线(AgentComposer) / 离线(TemplateComposer)  → emit progress(75%)
⑥ 渲染 report.tex（占位符替换 + 计算列插入 + 图表插入）→ emit progress(85%)
⑦ compiler.compile（自动修复 ≤3 次）→ emit progress(100%)
⑧ 写 events.jsonl + meta.json → 返回 {pdf_path, tex_path, warnings}
```

### 6.3 断网/LLM 不可用降级

`agent.compose` 捕获 LLM 错误（或 `initialize` 探明不可用）→ 自动切 `TemplateComposer`：
- 公式：从 `assets/<实验名>/formulas.json` 素材库取**已验证公式**（KaTeX 文本）；
- 误差分析/结论：从素材库取模板段落 + 计算结果格式化填充；
- 计算/绘图/填表**完全不受影响**（它们本来就不依赖 LLM）；
- 产物中标记 `mode: offline`，用户可稍后一键"在线润色"。

---

## 七、Agent 设计（LangGraph）

### 7.1 图结构（v1：线性 + 条件修复）

```
state: {form, data, computed, plots, draft, tex, compile_result, retries}

parse → fill_check → compute(工具) → plot(工具) → compose(LLM) → render_tex
                                                            │
                                              compile(工具) ←┘
                                                            │
                                          compile_result.ok? ── yes → done
                                                            │ no
                                              retries < 3? ── yes → fix_log(LLM) → compile
                                                            │ no → failed(附修复建议)
```

### 7.2 工具注册（全部受守卫）

| 工具 | 说明 | 守卫 |
|---|---|---|
| `calc_columns` | 调 compute.calc | 类型白名单、NaN 检查 |
| `render_charts` | 调 plot.render | 仅按 chart.json |
| `ocr_table` | 调 ocr.recognize | 置信度阈值 |
| `compile_tex` | 调 compiler.compile | 输出文件校验 |

LLM 的输入是**计算结果摘要**（计算列数值、拟合参数、图表已生成路径），输出契约是 JSON：`{formula_tex: [...], error_analysis: str, conclusion: str, edits: [{section, delta}]}`。禁止 LLM 直接产出数值。

### 7.3 提示词组装（系统提示三层）

1. **素材库事实层**：`assets/<实验名>/facts.json`（本实验的物理公式、量纲、常见误差来源，只读事实，不允许编造）；
2. **模板正文层**：principle.txt 原文（"正文 100% 来自模板，Agent 只做细微调整"）；
3. **数据摘要层**：计算列结果 + 拟合参数 + 图表清单。

离线时第 1、2 层仍可用 → 直接走 TemplateComposer。

### 7.4 双驱动实现

```
compose.py
├─ AgentComposer（在线）：LangGraph 图 + LLM，依据事实动态写公式、微调文字
└─ TemplateComposer（离线/兜底）：素材库公式 + 模板段落 + 结果格式化填充
两者实现同一 IComposer 接口 —— 换实现即切换，符合 dsh"agent loop 可替换"借鉴。
```

---

## 八、模板规范（细化）

### 8.1 三文件

```
templates/<实验名>/
├─ principle.txt    # 目的/原理/步骤等固定正文（原样搬运，Agent 只微调）
├─ report.tex       # LaTeX 模板（占位符）
└─ chart.json       # 图表参数规范（模板声明，agent 不自行发挥）
```

### 8.2 占位符文法（正式定义）

| 占位符 | 正则 | 替换规则 |
|---|---|---|
| 个人信息 | `\{\{(姓名\|学号\|学校\|班级)\}\}` | 来自 settings.personal，缺失则留空并警告 |
| 数据表 | `\\datatable\{([^}]*)\}` | 参数：`name=数据1, rows=5, cols={I/mA, U/V}` → 渲染为表格（LaTeX tabular） |
| 计算列 | `\\calc\{([^}]*)\}` | 参数：`type=mean, col=U/V, round=3` → 数值来自计算引擎，格式经 latex_formatter |
| 插图 | `\\plot\{([^}]*)\}` | 参数：图表名 → `\includegraphics{figures/<name>.png}` + 标题 |
| 章节块 | `\\section\{([^}]*)\}` | 映射到表单 sections，正文从 principle.txt 对应段落取 |

### 8.3 示例 report.tex（节选）

```latex
\documentclass[12pt]{ctexart}
\usepackage{graphicx, booktabs, amsmath}
\title{物理实验报告：{{学校}} \quad {{姓名}} \quad {{学号}}
\begin{document}
\maketitle
\section{实验原理}
（principle.txt 原理段落原文）
\section{数据记录}
\datatable{name=数据1, rows=5, cols={I/mA, U/V}}
\section{数据处理}
拟合斜率：\calc{type=slope, x=I/mA, y=U/V, round=3}
\section{误差分析}
% 误差分析正文由 compose 生成（公式 + 分析）
\section{结论}
% 结论正文由 compose 生成
\begin{figure}[htbp]
\centering
\plot{U-I曲线}
\end{figure}
\end{document}
```

---

## 九、LaTeX 编译与自动修复

### 9.1 编译流程

```
xelatex -interaction=nonstopmode -halt-on-error -output-directory=<out> report.tex
→ 第 1 遍失败 → 解析 .log（错误定位：行号/宏包/缺失文件/编码）
→ 修复规则表匹配 → 生成修复动作 → 重试（≤3 次）
→ 成功 → 第 2 遍（交叉引用/目录）→ 校验 PDF 存在且非空
```

### 9.2 常见错误 → 修复动作表（v1 内置规则）

| 日志特征 | 修复动作 |
|---|---|
| `File ... not found` / `! LaTeX Error: File` | 检查 \includegraphics 路径/文件名与 figures/ 是否一致；补充扩展名 |
| `Undefined control sequence` | 记录宏包缺失线索 → 添加对应 `\usepackage`（白名单内） |
| `Missing $ inserted` / `Extra }` | 交给 LLM 读 .log 修正公式片段（在线模式） |
| `Package ... not installed` | 提示用户安装 texlive 包，给出包名 |
| 中文乱码 | 检查 ctexart + xelatex 引擎 + UTF-8 编码 |

超出规则表 → 在线模式：LLM 读 .log 定位并给修复建议；离线：直接返回错误位置 + 人类可读建议。

---

## 十、OCR 集成设计（RapidOCR）

```
流程：图片 → RapidOCR(PP-OCRv5 onnx) → [[坐标框, 文本, 置信度], ...]
  → 按 y 坐标聚类成行、按 x 坐标排序成列
  → 表头匹配表单 columns（模糊匹配：忽略单位符号、空格）
  → 数值解析（去逗号/百分号/±），置信度 < min_confidence 的单元格标记"待确认"
  → 返回 {table, confidence, uncertain_cells} → 前端高亮让用户核对
```

- 首次调用懒加载模型（~30-50MB），之后常驻内存；
- 表单无表头匹配时，按"首行最可能为表头"推断，并让用户确认；
- OCR 产出**只作为表单填充预填**，进入计算前仍走 data 校验守卫。

---

## 十一、Electron 侧设计

### 11.1 页面路由

| 路由 | 页面 | 职责 |
|---|---|---|
| `/` | 模板库 | 导入/删除模板、卡片展示、进入表单 |
| `/form/:exp` | 表单页 | 字段/数据表录入（手填、Excel 粘贴、OCR 拍照）、计算列预览、图表预览 |
| `/source/:reportId` | 源码查看 | report.tex 高亮查看 + 事件日志回放 |
| `/settings` | 设置 | 个人信息、LLM、编译、OCR |

### 11.2 IPC 映射（preload 暴露）

```
window.labReport = {
  listTemplates(), importTemplate(path), parseTemplate(exp),
  submitForm(exp, formData),         // → report.generate（含进度事件订阅）
  cancelGenerate(), listReports(), readReport(id),
  getSettings(), saveSettings(partial),
  ocrRecognize(imagePath)
}
```

主进程持有 `agent-host.ts` 单例，封装 RPC 请求/事件分发；渲染进程只通过 preload 调用。

### 11.3 关键实现点

- Excel/CSV 粘贴：剪贴板文本解析（TSV/逗号分隔）→ 匹配表格列 → 预填；
- 表格编辑：React 受控组件 + 行增删；粘贴自动扩行；
- 报告生成中：进度条 + 阶段文案（事件驱动）；可取消；
- 源码查看：monaco/简单高亮 + events.jsonl 时间线。

---

## 十二、安全与数据真实性

| 项 | 机制 |
|---|---|
| LLM API Key | 主进程 safeStorage/DPAPI 加密存储，仅存在于 settings.json 密文 |
| 数据边界 | 全部本地，无任何云端上传（素材库/模板/产物均本地） |
| 数值真实性 | 守卫流水线（§4.2）：LLM 无任何数值产出通道 |
| 编译安全 | xelatex 以受限参数运行（不执行 shell、不读外部文件），产物目录隔离 |
| 权限门 | 复用 windows-app-maker 权限门：敏感操作（导入外部模板/覆盖文件）需确认 |
| OCR 隐私 | 识别在本地完成，图片不上传 |

---

## 十三、开发里程碑（M1–M8）

| 里程碑 | 内容 | 验收 | 参考 |
|---|---|---|---|
| **M1 骨架+协议** | electron-vite 工程、Python 子进程、JSON-RPC 握手/心跳、initialize 环境检查 | 前后端能互发消息；环境检查通过 | windows-app-maker 骨架、§3 |
| **M2 模板+表单** | template_parser（三文件→表单）、模板库管理、表单页（手填） | 导入模板→出表单→能填数 | §5.1 §8 |
| **M3 计算+绘图** | common/ 接入、compute.calc、plot.render、守卫流水线 | 计算列正确、图表生成、NaN 被拦截 | §4.2 §4.4 |
| **M4 Agent 组稿** | LangGraph 图、工具注册、提示词组装、AgentComposer | 在线生成公式+微调文字；输出 JSON 契约 | §7 |
| **M5 编译+修复** | xelatex 编译、日志解析、自动修复 ≤3 次、源码查看页 | 模板样例能出 PDF；常见错误能自愈 | §9 |
| **M6 OCR** | RapidOCR 集成、坐标排序、行列对齐、待确认高亮 | 拍照识别能预填表单 | §10 |
| **M7 界面完善** | Excel 粘贴、离线降级、历史回放、设置页 | 全流程无卡点；离线可出报告 | §5.5 §11 |
| **M8 打包** | electron-builder 打包、Python 运行时随包（PyInstaller）、首次启动检查 | 安装包在干净机器可运行 | §3.1 |

**里程碑依赖**：M3 依赖 M1/M2；M4 依赖 M3；M5 依赖 M2/M3；M6 独立可并行；M7 依赖 M4/M5/M6；M8 最后。

---

## 十四、风险与待定项

| 风险/待定 | 影响 | 对策 |
|---|---|---|
| xelatex 依赖本机 TeX 发行版 | 干净机器不可用 | M8 提供环境检查 + 引导安装（MiKTeX/TeXLive 精简） |
| OCR 精度数据（98%+）引自第三方对比 | 实际精度未知 | M6 用真实照片回归测试，必要时调阈值/换引擎 |
| 模板解析器复杂度 | 工作量最大头 | M2 先支持 v1 占位符子集，其余迭代 |
| 素材库范围（21 全做 or 3-5） | 影响离线兜底质量 | 先做 3-5 个高频实验，其余按需 |
| LangGraph + onnxruntime 打包体积 | 安装包偏大 | PyInstaller 按需裁剪 + 首次启动按需下载模型（可选） |
| 21 个实验的 generate.py 提炼 | 工作量不确定 | 优先复用公式/图表规格，思考题延后 |

---

## 十五、待用户确认的决策点

1. **模板导入细节**：v1 是否只支持本地文件夹导入（三文件），还是也支持 zip；
2. **素材库范围**：首批 3-5 个实验选哪几个（建议：伏安法、牛顿环、分光计、声速、霍尔效应）；
3. **LLM 接入**：默认 OpenA I 兼容接口（可填 DeepSeek/豆包等），是否还需要内置其他厂商预设；
4. **离线兜底质量**：是否接受"离线报告无 AI 微调"（公式/结论为素材库标准版）。

---

## 十六、双产物架构决策（2026-09-07 确认）

**决策**：Python 生成实验报告（Word），TeX 生成一份论文（PDF），双产物并存。

`
数据录入（数据.xlsx，复用锁定配置）
  ├─→ Python generate.py → _compute() → common/docx_report.py → 实验报告.docx（现有路线保留）
  └─→ Python generate_latex.py → _compute()（复用）→ 填充 .tex 模板（变体库组合）→ xelatex → 论文.pdf（新增）
`

**分层结论**：
- 数据层、计算层（_compute、common 库）完全复用，不搬迁；
- 仅排版层新增 LaTeX 通道（.tex 填充器 + xelatex 编译），Word 通道保留作保底；
- 防重复机制由"变体库 + 错落组合"承担（用户提供 .tex 模板骨架，助手生成多份章节描述）。

**变体库结构**：variants/<实验名>.json，每个章节多份描述，生成时随机/按种子组合。
**模板占位符**：%%VARIANT:章节名%%（变体插入）、%%DATA:键名[:格式]%%（数据插入）。
**编译**：main.js spawn xelatex（-interaction=nonstopmode），非阻塞 + 日志回传 + PDF 预览。

**待办**：用户提供 .tex 模板 → 生成变体库 → generate_latex.py 填充器 → main.js 编译通道 → 试点（静电场/拉伸法）→ 批量铺开。
