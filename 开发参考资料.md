# 实验报告自动编写应用 · 开发参考资料

> 版本：v1.0 ｜ 日期：2026-09-06
> 来源：本地工具/仓库盘点（E:\A-my-tools、E:\agent-project\useful-skills 等）
> 用途：开发本应用时的**参考索引**，按开发阶段查阅；不构成对任何仓库的整套采用建议。

---

## 一、资源总览与优先级

| 资源 | 本地路径 | 参考价值 | 何时用 |
|---|---|---|---|
| DeepSeek Harness (dsh) | `E:\A-my-tools\agent-framework-main\deepseek-harness-master` | ★★★★★ 架构思想 | 模块划分、Agent 流程、历史记录、配置分层设计 |
| Anthropic skills-main | `E:\A-my-tools\skills-main\skills-main` | ★★★★☆ 文档技能实现 | PDF/Word/Excel 输出模块、SKILL.md 写法 |
| awesome-design-md | `E:\A-my-tools\agent-framework-main\skill\awesome-design-md-main` | ★★★☆☆ UI 设计规范 | 界面开发（模板库/表单/设置） |
| i-have-adhd | `E:\agent-project\useful-skills\i-have-adhd-main` | ★★★☆☆ 输出风格 | Agent 组稿风格约束、日常编码体验 |
| skills-main-master（Matt Pocock） | `E:\A-my-tools\skills-main-master\skills-main` | ★★☆☆☆ 工程技能 | 日常编码技能（挑着用） |
| ECC | `E:\A-my-tools\agent-framework-main\ECC-main` | ★★☆☆☆ 编码配置合集 | 用 Claude Code/Cursor 时挑 best practices |
| claude-plugins-official | `E:\A-my-tools\claude-plugins-official-main\claude-plugins-official-main` | ★☆☆☆☆ 插件市场 | 装 Claude Code 插件时（无需本地副本） |
| cursor-plugins-main | `E:\A-my-tools\cursor-plugins-main\plugins-main` | ★☆☆☆☆ | 仅用 Cursor 时按需 |
| plugins-main（Codex） | `E:\A-my-tools\plugins-main\plugins-main` | ★☆☆☆☆ | 仅用 Codex 时按需 |
| dsh-cost-meter | `E:\A-my-tools\dsh-cost-meter-master\dsh-cost-meter-master` | ☆ 无用 | 不用 dsh 则忽略 |
| docker/ | `E:\A-my-tools\docker` | ☆ 已完成 | Docker Desktop 安装器，装完即弃 |

**核心结论**：这些仓库全部是开源"AI 编码工具装备库"，不是可直接引入的依赖。价值在于**借鉴设计思想与实现细节**，开发时按需翻阅对应章节即可。

---

## 二、核心架构参考：DeepSeek Harness（dsh）

### 2.1 是什么

DeepSeek AI 官方开源 agent harness（MIT），底层基于 Cordis 插件框架（"一切皆插件"），46+ 个 npm 包、pnpm monorepo。当前为 developer preview，官方声明会有破坏性变更。

- 关键文档（本地可读）：`docs/architecture.zh.md`、`docs/cordis-primer.zh.md`、`docs/agent-lifecycle.zh.md`、`docs/tool-execution-pipeline.md`、`docs/capability-seams.zh.md`、`docs/event-producer-consumer.zh.md`
- 官方文档站：https://deepseek-harness.github.io/deepseek-harness/

### 2.2 本应用可借鉴的 7 个思想（重点）

| # | dsh 架构思想 | 本应用落地借鉴 |
|---|---|---|
| 1 | **一切皆插件**：模型适配器、工具注册表、会话日志、agent loop 本身都是插件；注册是副作用，插件卸载时自动撤销，无特权内核 | compute / plot / ocr / compiler 全部做成"接口 + 可替换实现"：换 OCR 引擎、换 LaTeX 编译器、新增实验类型 = 挂一个模块，不动核心 |
| 2 | **Seam 三件套**：Service Definition（接口）+ Service Provider（实现）+ Consumer（使用方，通常是工具）三位一体 | 先定义 `IComputeEngine` / `IPlotRenderer` / `ICompiler` / `IOcrEngine` 接口，再各自实现，Agent 只面向接口编程 |
| 3 | **守卫式工具流水线**：`tools/pre-execute → execute → post-execute` 瀑布事件，所有工具执行被把关 | **数值真实性落地机制**：计算/绘图是"受守卫的确定性工具"，Agent 只能调用、只能写文字，从机制上杜绝 LLM 编造（对应方案原则 1） |
| 4 | **三类事件**：会话事件（append-only 持久）、agent 事件（实时拦截轮次）、能力事件（seam 上附加策略） | 表单→计算→绘图→组稿→编译用事件解耦；"模型可见即已记录"，可审计 |
| 5 | **append-only 会话日志**：模型请求可见的一切都能从日志重建 | outputs/history 升级为事件日志：输入→计算→组稿→编译全程可回看、可恢复、可核对 |
| 6 | **profile + patch 配置分层**：组合包按序叠加，patch 按 id 覆盖 | settings.json（全局个人信息/模型配置）+ templates/（实验模板层）分层，可叠加可覆盖，互不污染（对应方案原则 6） |
| 7 | **agent loop 本身是插件**：驱动器可整体替换 | 在线 Agent ↔ 离线"纯模板替换"两个驱动实现，天然支撑断网降级（对应方案第七章降级策略） |

### 2.3 不建议整体采用的原因（备忘）

- dsh 是 TS/Node 完整 harness，与本应用（Electron + Python LangGraph + stdio JSON-RPC）语言栈错位；
- developer preview，破坏性变更频繁；
- 46+ 包、pnpm monorepo，作为运行时依赖过重；
- 其 Python SDK 只是把 dsh 作为子进程驱动，不是可嵌入的 Python agent 库。

### 2.4 可顺手借鉴的"新行为归属位置"（摘自架构文档）

| 目标 | 机制（dsh 做法） | 本应用对应 |
|---|---|---|
| 添加面向模型的能力 | 在 `ctx.tools` 注册，schema 加入提示词组装 | 计算/绘图工具的 schema 声明进 Agent 提示词 |
| 拦截请求/工具/轮次 | 用 `agent/*` 或 `tools/*` 事件 | 编译失败自动修复的钩子（≤3 次） |
| 添加模型可见上下文 | `agent.inject()` 落到下一次请求 | 素材库公式事实注入组稿提示词 |
| 添加持久会话状态 | 扩展 SessionEventMap，从日志渲染 | 历史报告字段扩展 |

---

## 三、文档生成技能参考：Anthropic skills-main

### 3.1 是什么

Anthropic 官方 skills 仓库：技能示例（创意/开发/企业）+ `skills/docx`、`skills/pdf`、`skills/pptx`、`skills/xlsx` 四个**生产级文档技能源码**（source-available，非开源）+ Agent Skills 规范（`spec/`）+ 技能模板（`template/`）。

### 3.2 与本应用的对应关系

| 本应用模块 | 参考对象 | 参考内容 |
|---|---|---|
| xelatex 编译 + PDF 交付 | `skills/pdf` | 复杂文档技能如何组织指令/脚本/资源 |
| Excel·CSV 粘贴解析 | `skills/xlsx` | 表格数据读取与格式处理细节 |
| Word 公式渲染（方案中 `docx_report.py` 备选） | `skills/docx` | Word 文档生成与公式处理的工程做法 |
| 模板库/素材库的组织 | `template/` + `spec/` | SKILL.md 的标准写法（name/description/元数据规范） |

### 3.3 用途提醒

- 这是"复杂技能怎么写"的**参考教材**：每个技能自带 `SKILL.md`（指令 + 元数据）+ 脚本 + 资源，结构清晰；
- 本应用如要把"21 个实验"做成可复用的实验技能/素材单元，可参照该结构设计 `templates/<实验名>/` 目录规范。

---

## 四、UI 设计参考：awesome-design-md（DESIGN.md）

### 4.1 是什么

VoltAgent 的 awesome-design-md：**73 个网站的设计规范分析文件**（DESIGN.md 格式，源自 Google Stitch 概念）。DESIGN.md 是一个纯文本设计系统文档：把设计风格（配色/排版/组件/规则）写成 markdown，AI 智能体读了就能生成风格一致的 UI。

### 4.2 用法（开发界面时）

1. 从 `design-md/` 里挑一个风格贴近"简洁工具型"的参考；
2. 把 DESIGN.md 放进项目根目录；
3. 对 AI 说"按这个设计规范做 XX 页面"；
4. 生成结果风格一致性会明显更好，无需 Figma 导出或 JSON schema。

### 4.3 适用页面

模板库列表页、数据录入表单页、源码查看页、设置页、报告预览页。

---

## 五、输出风格参考：i-have-adhd

### 5.1 是什么

ayghri/i-have-adhd：ADHD 友好输出技能。核心规则：**Action first（先给动作）、Steps numbered（步骤编号）、No 'Hope this helps!'（无废话）**。支持 Claude Code / Cursor / Codex / OpenCode / Qwen / Kimi / Gemini 多端（各有 manifest/extension 配置），安装说明在 `INSTALL.md`。

### 5.2 与本应用的关系

- **Agent 组稿（compose.py）的风格约束参考**：微调误差分析/结论文字时，采用"直接、编号、无套话"的输出规范，避免生成"希望以上分析对你有帮助"类废话；
- **开发体验**：装到日常编码工具中，AI 回答风格贴合开发者偏好（先给可落地步骤）。

---

## 六、其他备用资源（低优先级）

| 资源 | 说明 | 何时翻 |
|---|---|---|
| ECC（`agent-framework-main/ECC-main`） | ecc-universal v2.2.1：面向 Codex/OpenCode/Cursor/Gemini/Claude Code 的 skills/hooks/rules/MCP 约定合集，`npx ecc-universal setup` | 用 Claude Code/Cursor 写代码时挑 best practices，不整套照搬 |
| claude-plugins-official | Anthropic 官方 Claude Code 插件市场目录，`/plugin install {name}@claude-plugins-official` | 需要 Claude Code 插件能力时，直接走 marketplace，无需本地副本 |
| cursor-plugins-main | Cursor 官方插件：Teaching / Continual Learning / Team Kit / Thermos / PR Review Canvas / Docs Canvas / Orchestrate / pstack / Advisor 等 | 仅当使用 Cursor 时按需拷贝单个插件 |
| plugins-main（Codex） | OpenAI Codex 官方插件示例：figma / notion / build-web-apps / build-macos-apps / expo 等 | 仅当使用 Codex 时参考 |
| skills-main-master | Matt Pocock"Skills For Real Engineers"：小而可组合、可改的工程技能 | 日常编码技能灵感，挑着用 |
| dsh-cost-meter | dsh 会话费用统计插件 v1.7.12 | 不用 dsh 则无价值，忽略 |
| docker/ | Docker Desktop 安装器 + wsl-data + setup-after-reboot.ps1 | 一次性安装物，已完成使命 |

---

## 七、开发路线图 → 参考资源对照

| 路线步骤（方案第九章） | 首要参考 |
|---|---|
| 1. 建项目骨架（复制 windows-app-maker 结构） | §2.2 思想 1/2：模块接口先行（compute/plot/ocr/compiler 先定义接口）；复用 windows-app-maker 的 main/preload/renderer 三段式 |
| 2. 接入 物理实验.zip 的 common/ 库 | 无外部参考，直接复用（uncertainty/regression/latex_formatter） |
| 3. 模板解析器（三文件 → 表单） | §2.2 思想 6（配置分层）、§3.3（SKILL.md 目录结构规范） |
| 4. 计算引擎 + 绘图模块 | §2.2 思想 3（守卫式流水线：计算/绘图为受守卫确定性工具） |
| 5. Agent 组稿（公式编写 + 微调） | §2.2 思想 7（在线/离线双驱动）、§五（i-have-adhd 输出风格） |
| 6. OCR 集成（RapidOCR） | RapidOCR 官方仓库（方案第十章已列） |
| 7. xelatex 编译 + 自动修复 | §3.2（skills/pdf 参考）、dsh 的 tools/* 事件拦截思想（§2.4） |
| 8. 界面（模板库/表单/预览/设置） | §四（DESIGN.md 设计规范） |

---

## 八、快速查阅卡（一句话备忘）

- **dsh 架构** → 模块全部接口化可替换；计算/绘图做成受守卫的确定性工具；历史报告事件化；配置分层。
- **Anthropic skills-main** → 写文档类模块前先翻 docx/pdf/xlsx 三个源码；SKILL.md 规范在 spec/。
- **DESIGN.md** → 做界面前选一个 design-md 放进项目根，AI 生成 UI 风格更一致。
- **i-have-adhd** → Agent 组稿风格：action first、编号步骤、无废话。
- **ECC / claude-plugins / cursor-plugins / codex-plugins** → 按主力编码工具按需取用，不整套照搬。
- **dsh-cost-meter / docker** → 与本项目无关，忽略。
