# 实验报告自动编写应用 —— 架构改造方案（方式三：结构化数据模型）

> 本文档是「方式三」改造的**完整工作指导**，供执行者直接照做。
> 文档自包含，不依赖任何外部对话上下文。执行前先通读 §0~§4，再按 §7 分阶段推进。
> 项目根：`E:\agent-project\labreport-writer`（下称「项目根」）。

---

## 0. 背景与目标

### 0.1 现状问题（为什么要改）

当前应用是一个 Electron 桌面程序，用户在一个「Excel 网格」里填物理实验测量数据，点击生成报告后调用每个实验的 Python 脚本 `generate.py`，通过 Word COM 生成 `.docx` 报告。存在三类纠缠的架构问题：

1. **锁定问题**：靠 `lock-regions.json` + `lock-config.json` + 启发式三级判定来标记「哪些格能填」，但语义自相矛盾，导致部分数据格被误锁死（用户已人工校对了复摆实验确认此问题）。
2. **数据读取问题**：`generate.py` 里数据坐标**硬编码**（`read_cell(excel, "B2")`），改 xlsx 结构就串列。
3. **单一事实来源缺失**：渲染、校验、生成三条路各自维护一份「数据在哪」的认知，互不知情。

### 0.2 目标（方式三：结构化数据模型）

**让数据脱离表格独立存在。** 每个实验的数据真相是一份结构化 JSON（`data.json`），由一份 `schema.json` 定义它有哪些字段。Excel 表格降级为「可选导出视图」，不再是数据真相。

一句话：**把「在 Excel 网格里填数据」改成「做填空题」——题面由 schema 定义，答案存在 data.json，改表格布局不影响数据、不影响读取。**

### 0.3 达成后消除的问题

- 锁定系统整体删除（表单里标签是静态文字、输入框是输入控件，天然不需要锁定）。
- 坐标硬编码消失（脚本读 `data["g0"]`，不读 `B2`）。
- 启发式校验消失（schema 自带 type/required/长度，校验变简单）。
- 改结构不再影响读取（布局与数据解耦）。

---

## 1. 现状架构速览（供执行者建立心智模型）

```
Electron 主进程 main.js
  ├─ 窗口管理 / 窗口控制 IPC
  ├─ 实验扫描 scan-experiments
  ├─ Excel 读写 read-excel / write-excel（xlsx 库 + 可选 openpyxl）
  ├─ 数据校验 check-data（spawn check_data.py）
  ├─ 锁定配置 read/write-lock-config、read-lock-regions
  ├─ 报告生成 run-generate / cancel-generate（spawn generate.py）
  ├─ docx 预览 docx-to-html / read-docx-buffer
  ├─ AI 对话 ai-chat
  └─ 变体 load/save-variants

预加载 preload.js → contextBridge 暴露 window.labAPI.*

渲染进程 src/renderer.js + index.html + style.css
  ├─ 实验列表 / 分类 / 搜索
  ├─ Excel 表格渲染与编辑（renderDataTable，含合并单元格）
  ├─ 锁定机制（锁定编辑模式、每格样式、三级判定）
  ├─ 结构编辑模式（合并/拆分/行高/列宽）
  ├─ 数据校验异常高亮
  ├─ 报告生成 / 批量生成 / 预览
  ├─ AI 润色 / 变体组合
  └─ 学生信息 / 设置

Python 子进程 物理实验/实验脚本/<实验>/generate.py
  ├─ _create_template()：openpyxl 生成 数据.xlsx 模板
  ├─ _compute(excel)：read_cell/read_row/... 读 xlsx → 计算
  └─ _generate_docx()：common/docx_report.py（Word COM）写报告

公共库 物理实验/实验脚本/common/
  ├─ docx_report.py（Word COM 封装，核心，保留）
  ├─ uncertainty.py / regression.py / latex_formatter.py / plot_utils.py（计算/绘图，保留）
  ├─ variants.py（变体组合，保留）
  ├─ excel_reader.py（读 xlsx，将删除）
  └─ excel_rich_text.py（写 Excel 富文本，将删除）
```

---

## 2. 目标架构（方式三）

```
Electron 主进程 main.js
  ├─ 窗口 / 窗口控制
  ├─ 实验扫描 scan-experiments
  ├─ 模型读写 read-schema / read-data / write-data（JSON，替代 Excel 读写）
  ├─ 模型校验 validate-data（spawn validate_schema.py，替代 check_data）
  ├─ 报告生成 run-generate / cancel-generate（spawn generate.py，读 data.json）
  ├─ docx 预览 / AI 对话 / 变体读写
  └─ （可选）导出 export-xlsx（data.json → 数据.xlsx 视图）

渲染进程：schema 驱动的「表单渲染器」替代「Excel 网格渲染器」
  ├─ 实验列表 / 学生信息 / 设置（保留）
  ├─ 表单渲染：读 schema → 渲染参数区（标签+输入框）+ 数据区（array/matrix 表格）
  ├─ 模型校验（必填/类型/范围，替代异常格高亮）
  ├─ 报告生成 / 预览 / AI / 变体（保留）
  └─ （锁定、结构编辑、Excel 网格渲染 全部删除）

Python 子进程 generate.py
  ├─ _compute(data)：读 data dict → 计算（计算逻辑不变，只改数据来源）
  └─ _generate_docx(data)：common/docx_report.py 写报告（完全不变）

公共库 common/
  ├─ docx_report.py / uncertainty.py / regression.py / latex_formatter.py / plot_utils.py / variants.py（保留）
  └─ 新增 data_io.py（load_data/save_data）
```

---

## 3. 核心数据结构规范（务必严格遵守）

### 3.1 schema.json（每实验一份，放在实验目录下，与 generate.py 同级）

```jsonc
{
  "实验": "重力加速度的测量（复摆）",
  "groups": [
    {
      "name": "实验参数",
      "fields": [
        {"key": "g0",         "label": "重力加速度 g0", "type": "number", "unit": "m/s²", "default": 9.797, "required": true},
        {"key": "delta_inst", "label": "仪器误差 Δ仪",  "type": "number", "unit": "s",    "default": 0.005, "required": true}
      ]
    },
    {
      "name": "测量数据",
      "fields": [
        {"key": "pivot",  "label": "支点位置",    "type": "array",  "length": 9, "unit": "cm"},
        {"key": "period", "label": "10周期测量",  "type": "matrix", "rows": 8, "cols": 9, "unit": "s"},
        {"key": "h",      "label": "h/cm",        "type": "array",  "length": 9, "unit": "cm"},
        {"key": "T0",     "label": "对称点 T0",   "type": "number", "unit": "s", "required": true}
      ]
    }
  ]
}
```

**字段类型 `type` 枚举（第一版只支持这四种）：**

| type | 含义 | 必带属性 | data 中的形态 |
|------|------|---------|--------------|
| `number` | 单值 | — | 数值或 null |
| `array` | 一维数组 | `length` | 定长 list |
| `matrix` | 二维数组 | `rows`, `cols` | 定长 list[list] |
| `text` | 短文本（一般不用，学生信息单独管） | — | 字符串或 null |

**字段通用属性：**
- `key`：唯一键，**必须与 generate.py 中 `data["..."]` 的取值键完全一致**（这是唯一契约）。
- `label`：表单上显示的题面文字。
- `unit`：单位，展示用，可空字符串。
- `default`：默认值（`number` 用数值；`array`/`matrix` 可选）。
- `required`：是否必填（默认 false）。必填且值为 null/空 → 校验报 missing。

### 3.2 data.json（每实验一份，实验目录下，数据真相）

```jsonc
{
  "g0": 9.797,
  "delta_inst": 0.005,
  "pivot": [35.2, 35.1, 34.9, null, null, null, null, null, null],
  "period": [[...8×9 二维...]],
  "h": [null, null, null, null, null, null, null, null, null],
  "T0": 4.35
}
```

**空值约定（强制统一）：**
- `number` 未填 = `null`。
- `array` 未填 = 长度固定的 list，未填的项为 `null`（不能缩成空 list）。
- `matrix` 未填 = `rows×cols` 的 list[list]，未填的项为 `null`。
- `text` 未填 = `null` 或 `""`（二选一，实现时统一用 `null`）。

### 3.3 唯一契约

`schema.json` 的 `key` == `data.json` 的顶层键 == `generate.py` 里 `data["..."]` 的取值键。**三处必须一致**，这是整个方式的锚点。

---

## 4. 删除清单 / 保留清单（重点）

### 4.1 要删除的（整体删除，不再保留）

**根目录文件：**
| 文件 | 说明 |
|------|------|
| `lock-regions.json` | 锁定区域配置，废弃 |
| `lock-config.json` | 用户手动锁定，废弃 |
| `build_lock_regions.py` | 锁定配置生成器，废弃 |
| `check_data.py` | 启发式校验器，被 schema 校验替代 |
| `_scan_locks.py` | 锁定诊断脚本，废弃 |
| `_scan_numlocks.py` | 锁定诊断脚本，废弃 |
| `read_excel_style.py` | Excel 样式读取，废弃（不再需要底色判断） |
| `read-map.json` | 读取清单（迁移期临时用，迁移完成后删） |
| `scan_reads.py` / `verify_reads.py` | 坐标提取/自检工具（迁移期用，迁移完成后可删） |
| `backup-*` 目录 | 历史备份，可保留不动 |

**公共库 common/：**
| 文件 | 说明 |
|------|------|
| `common/excel_reader.py` | 读 xlsx，废弃（generate.py 不再读 xlsx） |
| `common/excel_rich_text.py` | 写 Excel 富文本，废弃（不再用 openpyxl 生成模板） |
| `common/output_writer.py` | 调试用文本公式输出，废弃 |
| `common/__init__.py` 中 `from .excel_reader import ...` 和 `from .excel_rich_text import ...` | 删除这两行导出 |

**渲染进程 src/renderer.js 中删除的模块（函数分组）：**
- Excel 渲染：`renderDataTable`、`loadExcelData`、`saveExcelData`、`reloadExcelData`、`renderSheetTabs`、`formatCellText`、`isSectionRow`、`isTitleRow`、合并单元格相关辅助。
- 锁定系统：`isCellLocked`、`isCellLockedGlobal`、`matchLockRegion`、`isAutoCalcCell`、`isAutoCalcCellGlobal`、`CALC_KEYWORDS`、`toggleLockEditMode`、`lockSelectedCell`、`unlockSelectedCell`、`saveLockConfig`、`loadLockConfig`、`loadLockRegions`、`getLockBgColor`、`adjustColorDepth`、`getLockCellStyle`、`getCellStyleConfig`、`setCellStyleConfig`、`syncStyleControlsToSelection`、`applyStyleSetting`、`idxToCol`（坐标转换，两处）。
- 结构编辑：`toggleStructEditMode`、`onStructCellClick`、`getStructRange`、`mergeSelected`、`unmergeSelected`、`clearMerges`、`setSelectedRowHeight`、`setSelectedColWidth`、`updateStructButtons`、`undoStruct` 等（方式三下表格布局由 schema 定义，不再需要 Excel 式结构编辑）。
- 数据校验异常高亮：`refreshDataCheck`（调 check_data 的部分）、`currentAbnormalMap`、`renderDataIssueBar`、`toggleIssueList`、`scrollToAbnormalCell`、`cellRef`（被 schema 校验 UI 替代）。
- 状态变量：`currentExcelData`、`currentSheetName`、`currentSheetMeta`、`lockRegions`、`manualLocks`、`isLockEditMode`、`isStructEditMode`、`abnormalCells`、`currentCheckSummary`、`lockColor/lockDepth/...`、`cellStylesMap`、`selectedLockCell`、`excelDataCache`、`structSel`、`structSel2`、`undoStack` 等（换成 schema/data 相关状态）。

**渲染进程 src/index.html 中删除：**
- 锁定工具栏 `#lockToolbar` 及其控件。
- 结构编辑工具栏 `#structToolbar` 及其控件。
- 数据异常提示条 `#dataIssueBar` 及其详情列表。
- Excel 表格容器 `#dataTableWrap`（换成表单容器 `#formWrap`）。
- sheet 切换标签 `#sheetTabs`（方式三下单实验可有多个 groups，用 group 分区代替 sheet）。

**主进程 main.js 中删除的 IPC handler：**
- `read-excel`、`write-excel`、`check-data`、`read-lock-config`、`write-lock-config`、`read-lock-regions`。
- 顶部 `const XLSX = require('xlsx')`（如不再需要则删）。

**预加载 preload.js 中删除的 API：**
- `readExcel`、`writeExcel`、`checkData`、`readLockConfig`、`writeLockConfig`、`readLockRegions`。

**generate.py（25 个）中删除：**
- `_create_template()` 整个函数（openpyxl 生成 xlsx 模板，不再需要）。
- 所有 `read_cell/read_row/read_column/read_range` 读取调用及配套的坐标常量、循环读取代码。
- `main()` 里「检查 xlsx 是否存在 → 建模板」的逻辑。

### 4.2 要保留的（原样保留，不重构）

**主进程 main.js 保留：**
- 窗口创建（`createWindow`，无边框、contextIsolation 等安全配置）。
- `findProjectRoot()`、`resolvePythonExe()`（Python 定位）。
- 窗口控制 IPC（`window-minimize/maximize/close`）。
- `scan-experiments`（实验列表扫描，可微调：报告文件、data.json 存在性）。
- `run-generate` / `cancel-generate`（生成调度 + 取消 + Word 进程清理，**核心，保留**）。
- `docx-to-html` / `read-docx-buffer`（报告预览）。
- `ai-chat`（AI 对话，OpenAI 兼容）。
- `load-variants` / `save-variants`（变体）。
- `data-modified` / `app-save-and-close` / `app-confirm-close`（未保存提示）。
- 日志 `log()`。

**预加载 preload.js 保留：**
- 除 4.1 列出删除项外的全部（`scanExperiments`、`openFile`、`runGenerate`、`cancelGenerate`、`onGenerateLog`、窗口控制、`docxToHtml`、`readDocxBuffer`、`aiChat`、`loadVariants`、`saveVariants`、`logEvent`、`setDataModified`、`onSaveAndClose`、`confirmClose`）。

**渲染进程保留：**
- 实验列表/分类/搜索、学生信息、设置、报告生成/批量生成/预览、AI 润色、变体组合、事件绑定框架、Toast/模态框。

**Python 公共库 common/ 保留：**
- `docx_report.py`（Word COM 报告写入，**最核心，完全不动**）。
- `uncertainty.py`、`regression.py`、`latex_formatter.py`、`plot_utils.py`（计算/格式化/绘图，不动）。
- `variants.py`（变体组合，不动）。

**generate.py 保留（25 个）：**
- `_compute()` 的**计算逻辑**：公式、不确定度、逐差法、线性回归、结果字典 `r` 的组装（**数据来源改成 data dict，计算结果字典的结构不变**）。
- `_print_results()`（stdout 打印）。
- `_generate_docx()` 的**报告写入逻辑**（Word COM 章节/公式/表格/图片），完全不变，只把入参从 excel_path 换成 data。

**其它保留：**
- `物理实验/实验脚本/common/docx_report.py` 依赖的 Word COM 机制。
- 学生信息环境变量 `LAB_STUDENT_NAME/ID/CLASS/DATE` 机制。
- 变体环境变量 `LAB_VARIANTS` 机制。
- `main.js` 生成调度里「退出码 0 但无 docx → 报缺失」的兜底。
- `python-runtime/`、`package.json` 的 Electron 框架。

---

## 5. 新增清单

| 新增 | 位置 | 说明 |
|------|------|------|
| `schema.json` | 每个实验目录下 | 数据模型定义（§3.1） |
| `data.json` | 每个实验目录下 | 数据真相（§3.2） |
| `common/data_io.py` | common/ | `load_data(path)` / `save_data(path, data)`，统一 JSON 读写（utf-8） |
| `validate_schema.py` | 项目根 | schema 校验器：输入实验目录 → 输出 missing/invalid（替代 check_data.py） |
| `migrate_xlsx.py` | 项目根（迁移期临时） | 一次性 xlsx→data.json 导入器，迁移完成后删除 |
| `export_xlsx.py` | 项目根（可选） | data.json → 数据.xlsx 导出视图 |
| `renderer.js` 新增 | src/ | `loadSchema`、`renderForm`、`readFormData`、`validateForm`、`showValidationIssues` |
| IPC 新增 | main.js + preload.js | `read-schema`、`read-data`、`write-data`、`validate-data`（+ 可选 `export-xlsx`） |
| index.html 新增 | src/ | 表单容器 `#formWrap`、校验提示条（替代 dataIssueBar） |

---

## 6. 分阶段实施流程（务必按顺序，每阶段验收后才进下一阶段）

### 阶段 0：冻结设计 + 选样板

- 阅读本文档 §3 数据结构规范，确认理解。
- 选定样板实验：**重力加速度的测量（复摆）**（目录名含括号，注意路径转义）。
- 产物：无（设计冻结）。

**验收**：无。

### 阶段 1：复摆样板端到端跑通（最重要，先打通一个）

1. 写 `物理实验/实验脚本/重力加速度的测量（复摆）/schema.json`（参照 §3.1，字段来自现有 generate.py 的 `_compute` 读取项）。
2. 写 `common/data_io.py`。
3. 写复摆 `data.json`（初始值从现有 `数据.xlsx` 人工/脚本提取，可为 null）。
4. 改复摆 `generate.py`：
   - 删 `_create_template`、删所有 `read_*` 调用。
   - `_compute(excel_path)` → `_compute(data)`，内部 `read_cell(...,"B3")` → `float(data["g0"])`。
   - `main()`：读 `data.json` → `_compute(data)` → `_generate_docx(data, DOCX_FILE)`。
   - **结果字典 `r` 的 key 保持不变**（variants 的 `%%DATA:key%%` 依赖它）。
5. 运行 `python generate.py`，确认生成 `重力加速度的测量（复摆）.docx`，内容与旧版一致（公式/表格/数值）。

**验收**：复摆从 data.json 生成报告成功，数值、公式、表格与旧 xlsx 版一致。

### 阶段 2：写通用工具

1. `validate_schema.py`：读 schema.json + data.json → 校验 required 缺失、类型错误、array/matrix 长度不符 → 输出 `{"ok", "missing":[{key,label}], "invalid":[{key,label,reason}]}`。参考旧 check_data.py 的输出协议（供 UI 复用）。
2. `migrate_xlsx.py`（一次性迁移，见 §7）：读 `migration-map.json`（key → 旧坐标）+ 现有 `数据.xlsx` → 生成 `data.json`。
3. `export_xlsx.py`（可选）：data.json → 数据.xlsx。

**验收**：校验器对「完整/缺失/类型错」三种 data 输出正确；迁移器能把复摆 xlsx 正确转成 data.json。

### 阶段 3：批量迁移 + 改造 25 个 generate.py

对**每个实验**依次：
1. 从现有 `generate.py` 的读取代码 + `_compute` 反推「key → 旧坐标」，写进 `migration-map.json`（或每实验临时清单）。
2. 跑 `migrate_xlsx.py` 生成 `data.json`（保留用户已填数据）。
3. 写 `schema.json`。
4. 改 `generate.py`（同阶段 1 第 4 步）。
5. 运行 `generate.py` 验证报告正确。

**验收**：25 个实验全部能从 data.json 生成报告，数值/公式/表格/图片正确，无异常。逐实验验证，不批量跳过。

### 阶段 4：渲染层表单化

1. main.js 新增 IPC：`read-schema`（读实验 schema）、`read-data`（读 data.json）、`write-data`（写 data.json）、`validate-data`（spawn validate_schema.py）。
2. preload.js 新增对应 API。
3. renderer.js：
   - `loadSchema`：选中实验时读 schema。
   - `renderForm`：按 schema 渲染——每个 `number` 渲染「label + 输入框 + unit」，`array` 渲染一行输入框，`matrix` 渲染 rows×cols 表格；标签和单位是静态文本（不可编辑）。
   - `readFormData`：从表单收集 → data 结构。
   - `validateForm`：前端即时校验（required/类型）+ 调 validate-data 后端校验。
   - 保存：`write-data`。
4. index.html：数据 tab 内容换成 `#formWrap` 表单容器，加校验提示条。
5. style.css：表单样式（标签列 + 输入框 + 表格）。

**验收**：复摆在应用里以表单形式呈现，能填、能存、能校验，能生成报告。

### 阶段 5：删除旧系统（执行 §4.1 完整删除清单）

1. 删根目录旧文件（lock-regions.json、lock-config.json、build_lock_regions.py、check_data.py、_scan_*.py、read_excel_style.py）。
2. 删 renderer.js 的锁定/结构编辑/Excel 渲染/异常高亮代码块。
3. 删 main.js 旧 IPC、preload.js 旧 API。
4. 删 common/excel_reader.py、excel_rich_text.py、output_writer.py，改 __init__.py。
5. 删 generate.py 的 `_create_template`（阶段 3 已删）。
6. `node --check main.js && node --check preload.js && node --check src/renderer.js` 语法检查。

**验收**：应用能启动，无锁定/Excel 网格残留，无引用报错（grep 确认无 `lock-`、`readCell`、`isCellLocked`、`renderDataTable` 等残留）。

### 阶段 6：回归 + 收尾

1. 25 实验全部从应用界面走一遍「填数据 → 生成报告」，确认正确。
2. （可选）export_xlsx 验证导出。
3. 删除迁移期临时文件（migration-map.json、migrate_xlsx.py、scan_reads.py、verify_reads.py、read-map.json）。
4. 更新 `package.json` 的 `build.files`（去掉 lock-*.json，加上 schema.json/data.json 的通配，参照现有 `物理实验/实验脚本/*/...` 写法）。
5. 打包 `npm run build:win` 验证。

**验收**：全部功能正常，打包可运行，无旧架构残留。

---

## 7. 数据迁移方案（阶段 2/3 关键）

现有 25 个 `数据.xlsx` 里用户已填的数据**不能丢**。迁移分两步：

1. **建迁移映射**：对每个实验，从旧 `generate.py` 的读取代码反推「key → 旧坐标」清单。可利用项目中已有的 `read-map.json`（已提取 102 条读取调用及语义名）作为起点，人工核对语义名与 schema key 对齐。
   - 例：复摆 `g0 ← read_cell(excel, 0, "B3")`、`pivot ← for col in "BCDEFGHIJ": read_cell(f"{col}9")`。
2. **跑导入器**：`migrate_xlsx.py` 读映射 + xlsx → 按坐标提取值 → 组装 data.json（array/matrix 按长度补 null）。

迁移完成后，坐标信息不再存在，xlsx 也不再是数据真相。

---

## 8. 关键文件改造指引（模板）

### 8.1 generate.py 改造模板

```python
# -*- coding: utf-8 -*-
"""<实验名> — 数据处理脚本（方式三：读 data.json）。"""
import math, os, sys
SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, os.path.dirname(SCRIPT_DIR))
from common import *
from common.docx_report import DocxReportWriter
from common.data_io import load_data
from common.variants import compose  # 有变体功能的实验才需要

# ── 常量（物理常量、仪器误差等，保留）──

def _compute(data: dict) -> dict:
    # 数据来源：data["g0"]，不再 read_cell
    g0 = float(data["g0"])
    pivot = [float(v) for v in data["pivot"] if v is not None]
    # ... 计算逻辑（公式/不确定度/回归）不变 ...
    return {"g0": g0, "T": T, ...}   # r 字典结构不变

def _print_results(r):
    # 保留，打印计算结果

def _generate_docx(data: dict, output_path: str):
    r = _compute(data)
    _print_results(r)
    doc = DocxReportWriter(output_path)
    doc.add_title("...")
    doc.add_student_info()
    # 变体（可选）
    variants = compose(SCRIPT_DIR, r)
    # ... 章节/公式/表格/图片 写入不变 ...
    doc.save(); doc.close()

def main():
    DATA_FILE = os.path.join(SCRIPT_DIR, "data.json")
    DOCX_FILE = os.path.join(SCRIPT_DIR, "<实验名>.docx")
    data = load_data(DATA_FILE)
    _generate_docx(data, DOCX_FILE)
    print(f"报告已生成: {DOCX_FILE}")

if __name__ == "__main__":
    main()
```

**注意**：`compose(SCRIPT_DIR, r)` 的 `r` 字典 key 必须保持与旧版一致，否则变体的 `%%DATA:key%%` 占位符渲染会错。

### 8.2 main.js 新增 IPC 模板

```js
ipcMain.handle('read-schema', (_, expPath) => { /* 读 expPath/schema.json */ });
ipcMain.handle('read-data', (_, expPath) => { /* 读 expPath/data.json */ });
ipcMain.handle('write-data', (_, expPath, data) => { /* 写 expPath/data.json */ });
ipcMain.handle('validate-data', (_, expPath) => {
  // spawn python validate_schema.py expPath PROJECT_ROOT，返回 {ok, missing, invalid}
});
```

---

## 9. 约束与坑（务必遵守）

1. **中文目录名/实验名含括号**（`重力加速度的测量（复摆）`），文件路径操作注意转义，JSON 读写用 `utf-8`（读取源码用 `utf-8-sig` 防 BOM）。
2. **Word COM 仅 Windows**：`docx_report.py` 完全不动，`generate.py` 必须 `doc.close()` 防 Word 进程残留（保留现有机制）。
3. **学生信息/变体环境变量**机制保留：`LAB_STUDENT_*`、`LAB_VARIANTS`。
4. **`main.js` 生成调度兜底**保留：退出码 0 但无 docx → 报「数据未填完整」。
5. **用户硬约束**（历史交接文档 §13）：只交 Word（.docx）、纯中文无英文摘要、实验原理按教材（不联网搜）、实验名用教材标准名、分类规则（力/光/电/其他）、不要任何「原创/mh」标签、改文件前先备份。
6. **实验目录名**：第 14 号实验目录名是「用冲击法测量**螺旋管**磁场分布实验」（螺旋管，非螺线管），代码引用勿写错。
7. **`r` 字典 key 契约**：改造 `_compute` 时结果字典 key 不变（见 8.1）。
8. **空值约定**统一为 `null`（见 3.2），array/matrix 长度固定。
9. **迁移不丢数据**：迁移器必须正确提取 xlsx 里用户已填的值（见 §7）。
10. **每阶段可回退**：改造前备份相关文件，命名 `backup-<时间戳>-<说明>`。
11. **改 JS 后** `node --check` 语法检查；改 Python 后单独运行验证。
12. **`package.json` build.files**：方式三后需把 `schema.json`、`data.json` 加入打包 files（参照现有 `物理实验/实验脚本/*/generate.py` 写法），去掉 `lock-*.json`、`数据.xlsx` 相关项（如不再打包 xlsx）。

---

## 10. 验收标准（最终）

1. 25 个实验均能从 `data.json` 生成正确 `.docx`（公式、三线表、图片、数值无误）。
2. 应用内表单能正确渲染 schema、填写、保存、校验（required/类型/长度），并生成报告。
3. 全项目 grep 无旧架构残留：`lock-regions`、`lock-config`、`read_cell`、`read_row`、`renderDataTable`、`isCellLocked`、`check_data`、`_create_template`、`XLSX`（除非 xlsx 导出仍用）。
4. `npm run build:win` 打包成功，打包版可运行并生成报告。
5. 数据迁移无丢失：迁移前后对比，用户已填数据完整保留在 data.json。

---

*方案版本：方式三 v1（2026-09-09）*
