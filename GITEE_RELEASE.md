# 发布更新流程（供开发者使用）

应用内置「检查更新」功能，支持两种更新源（设置 → 检查更新 中配置，可随时切换）：

1. **Gitee Release**（默认）：查询 `https://gitee.com/api/v5/repos/<用户名>/<仓库名>/releases/latest`；
2. **自定义清单地址**（推荐用于大安装包）：读取一份公网可访问的 `latest.json` 清单，安装包可放任意公网直链（国内对象存储等）。

## 方式一：Gitee Release（安装包 ≤ 免费版单文件限制时）

1. **改版本号**：编辑 `package.json` 的 `version` 字段，如 `1.2.0`；
2. **打包**：`npm run build:win`，产物为 `dist/实验报告编写 Setup <版本>.exe`；
3. **登录 Gitee**（gitee.com），进入仓库 → 左侧「发行版 / Releases」→「新建发行版」：
   - **标签**：填 `v1.2.0`（必须带 `v` 前缀、与版本号一致）；
   - **标题**：如「v1.2.0」；
   - **说明**：填写本次更新内容（会原样显示在应用内的更新弹窗里，支持换行）；
   - **附件**：上传上一步的安装包 `.exe`；
   - 点击「发布」。
4. 用户在应用内「设置 → 检查更新」即可发现新版本并**自动下载**。

## 方式二：自定义清单（安装包较大 / 有对象存储时，推荐）

适合安装包超过 Gitee 免费版单文件限制（约 100MB，本安装包约 164MB）的场景。
以**腾讯云 COS / 阿里云 OSS** 为例（国内高速、默认域名免备案、免费额度够用）：

1. 开通 COS/OSS，创建桶（Bucket），地域选国内（如广州 ap-guangzhou）；
2. 上传安装包到桶，获得公网直链，如：
   `https://labreport-1250000000.cos.ap-guangzhou.myqcloud.com/实验报告编写%20Setup%201.2.0.exe`
3. 在桶中放一份清单文件 `latest.json`：
   ```json
   {
     "version": "1.2.0",
     "notes": "本次更新内容：\n1. 修复……\n2. 新增……",
     "url": "https://labreport-1250000000.cos.ap-guangzhou.myqcloud.com/实验报告编写%20Setup%201.2.0.exe",
     "fileName": "实验报告编写 Setup 1.2.0.exe"
   }
   ```
4. 用户在应用「设置 → 检查更新」填写该清单的完整 URL（须为公网 http/https 地址，本地/内网地址会被拒绝）；
5. 之后每次发布：覆盖上传新安装包 + 更新 `latest.json` 即可，**无需重新打包应用**。

## 检查更新原理

- 有自定义清单地址：GET 清单 → 比对 `version` 与本地版本 → 有新版本即自动下载 `url`；
- 无自定义清单：请求 Gitee 公开 API 取最新 release → 比对 `tag_name` → 自动下载附件中的 `.exe`（无 exe 时取 zip）；
- 下载完成后自动打开安装程序；可随时点「稍后再说」取消下载；
- 安全检查：更新/下载地址仅允许公网 http/https（自动拒绝 localhost、内网、私有与保留地址，域名解析后再次核验）。

## 注意事项

- 首次使用需在「设置 → 检查更新」中填写更新源（Gitee 用户名/仓库名，或自定义清单地址），保存后持久生效；
- 自定义清单 URL 与安装包直链必须为公网可访问地址，否则应用会拒绝访问；
- 若 Gitee 与 GitHub 双平台发布，本功能只认 Gitee 与自定义清单（国内优先）。
