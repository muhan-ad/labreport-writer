# 实验报告自动编写应用 · 方案设计

> 版本：v1.0 定稿 ｜ 日期：2026-09-06
> 形态：**Electron 桌面应用**（本地、免费、数据自持、可跑 Python）

---

## 一、项目概述

面向**大学物理实验**报告自动编写的桌面应用：

- 用户按填空方式录入实验数据（手填 / Excel 粘贴 / OCR 拍照识别）
- Agent 按模板微调报告文字、**动态编写公式**
- 误差分析自动完成（计算 + 数据图）
- 输出 **LaTeX 编译的 PDF**（保留 .tex 源码可查看）
- 数据全部本地保存，无服务器、0 成本

---

## 二、形态决策：桌面应用（而非网页）

| 维度 | 纯网页 | 桌面应用（Electron）✅ |
|---|---|---|
| 数据保持 | IndexedDB，清缓存/换浏览器即丢 | 本地文件，可导出备份 |
| 运行 Python | 仅 Pyodide（WASM，重、受限） | 直接 spawn Python 子进程 |
| 复用既有 Python 代码 | 需重写为 JS | **直接 import 复用** |
| LaTeX 输出 | 仅 KaTeX 近似 | 真 xelatex 编译（PDF + .tex） |
| 成本 | 0 | **同样 0**（开发工具免费、无服务器、数据本地） |
| 代价 | 手机可用 | 仅电脑 |

**结论**：需要跑 Python + 数据保持 → 桌面应用。0 成本目标不受影响。

---

## 三、总体架构

```
Electron + React + TypeScript + Python agent（复用 windows-app-maker 骨架）
│
├─ 技术栈
│   ├─ 前端：Electron + electron-vite + React 18 + TS
│   ├─ Agent：Python 3.10+ LangGraph（stdio JSON-RPC 子进程通信）
│   ├─ 计算：直接复用 物理实验.zip 的 common/ 库
│   ├─ 绘图：matplotlib（Python 侧）
│   ├─ 公式：LaTeX（KaTeX 语法书写，xelatex 编译）
│   ├─ OCR：RapidOCR（PP-OCRv5，本地离线）
│   └─ 输出：xelatex 编译 PDF + .tex 源码
│
├─ 目录结构（规划）
│   ├─ src/                    # Electron + React
│   │   ├─ main/               # 主进程：模板管理、编译、agent 子进程
│   │   ├─ preload/
│   │   └─ renderer/           # 模板库 / 表单页 / 源码查看 / 设置
│   ├─ agent/                  # Python
│   │   ├─ main.py             # stdio 协议（复用数模模式）
│   │   ├─ compute.py          # 计算引擎（复用 common/ 算法）
│   │   ├─ plot.py             # matplotlib 绘图
│   │   ├─ compose.py          # 组稿：占位符替换 + 正文搬运 + 微调
│   │   ├─ compiler.py         # xelatex 编译 + 失败自动修复
│   │   └─ ocr_bridge.py       # RapidOCR 接口
│   ├─ templates/              # 用户导入的模板库
│   ├─ assets/                 # 内置素材库（21 个实验）
│   ├─ settings.json           # 个人信息 / 模型 / 编译 / OCR 配置
│   └─ outputs/                # 产物：PDF + .tex + figures/
```

---

## 四、复用既有资源

### 4.1 复用 windows-app-maker（数模工具）骨架

- Electron 三段式（main / preload / renderer）+ electron-vite
- Agent 子进程通信模式（stdio JSON-RPC、事件广播）
- 模型配置加密（safeStorage / DPAPI）
- 权限门机制、技能系统

### 4.2 复用 物理实验.zip（西安电子科技大学物理实验课程工具）

| 模块 | 内容 | 复用方式 |
|---|---|---|
| `common/uncertainty.py` | A类/B类/合成/数值法不确定度传递 | ✅ 计算引擎直接 import |
| `common/regression.py` | 线性回归（斜率/截距/相关系数） | ✅ 直接 import |
| `common/latex_formatter.py` | 数值→LaTeX，有效数字对齐 | ✅ 直接 import |
| 21 个实验的 `generate.py` | 公式、数据处理逻辑、图表规格、思考题答案 | ✅ 提炼为**内置素材库** |
| `common/docx_report.py` | Word COM 公式渲染 | ⚠️ 依赖本机 Word，本方案以 LaTeX 输出为主 |

> zip 内 21 个实验：扭摆法、复摆、三线摆、薄透镜、分光计、牛顿环、光栅、迈克尔逊、单缝衍射、偏振光、声速、理想气体、RLC、伏安特性、电子束偏转、霍尔效应、劈尖干涉、灵敏电流计、电容高电阻、冲击法磁场、电子元件伏安

### 4.3 OCR 选型：RapidOCR（PP-OCRv5）

**为什么选它**（本地 / 免费 / 轻量 / 高精度全满足）：

| 指标 | RapidOCR | PaddleOCR | Tesseract | EasyOCR |
|---|---|---|---|---|
| 中文精度 | 98%+，数字 99%+ | 98.2% | 89.7% | ~97% |
| CPU 速度 | 30~100ms/张 | 417ms/张 | 182ms | 3~10s |
| 模型体积 | 14~20MB | 大 + paddle 重依赖 | 小 | PyTorch 重 |
| 依赖 | 仅 onnxruntime | paddlepaddle | 轻 | PyTorch |

```python
# ocr_bridge.py 核心实现（一行调用）
from rapidocr_onnxruntime import RapidOCR
ocr = RapidOCR()                      # 自动加载 PP-OCRv5 onnx 模型
result, _ = ocr("数据表照片.png")      # 返回 [[坐标, 文字, 置信度], ...]
```

- 一次性下载：pip 包 + 模型共 **~30-50MB**，装完永久离线
- 识别结果按文本框坐标排序 → 对齐表单数据表行列 → 用户核对后使用

---

## 五、模板规范（用户自行导入）

每个实验一个目录：`templates/<实验名>/`，含 3 个文件：

```
templates/<实验名>/
├── principle.txt     # ① 实验目的/原理/步骤等固定正文（原样搬运）
├── report.tex        # ② LaTeX 模板（占位符）
└── chart.json        # ③ 图表参数规范（模板声明，agent 不自行发挥）
```

### 5.1 占位符约定

| 占位符 | 作用 | 示例 |
|---|---|---|
| `{{姓名}}` `{{学号}}` | 个人信息（来自设置，不进表单） | `{{姓名}}` |
| `\datatable{...}` | 数据表声明 | `\datatable{name=数据1, rows=5, cols={I/mA, U/V}}` |
| `\calc{...}` | 计算列（Python 精确计算） | `\calc{type=mean, col=U/V, round=3}` |
| `\plot{图表名}` | 插图位置 | `\plot{U-I曲线}` |

### 5.2 计算列类型

| 类型 | 含义 |
|---|---|
| `mean` | 算术平均值 |
| `std` | 标准差 |
| `ua` | A类不确定度（s/√n） |
| `ub` | B类不确定度（仪器精度 ÷ √3） |
| `uc` | 合成不确定度 |
| `slope` / `intercept` / `r` | 线性拟合斜率 / 截距 / 相关系数 |

### 5.3 chart.json 示例

```json
{
  "图表": [{
    "name": "U-I曲线",
    "type": "scatter",
    "x": "电流I/mA", "y": "电压U/V",
    "fit": "linear",
    "error_bar": "y",
    "title": "U-I 特性曲线"
  }]
}
```

---

## 六、Agent 工作流（7 步）

```
① 导入模板 → 解析三文件生成表单
② 填数：手填 / Excel·CSV 粘贴 / OCR 拍照识别
③ 本地计算引擎算计算列（复用 common/ 库，Python 精确计算，杜绝 LLM 编造）
④ matplotlib 按 chart.json 绘图（拟合线 + 误差棒 + 单位）
⑤ Agent 在线：基于素材库公式事实 → 动态编写 KaTeX 公式 + 微调误差分析/结论
⑥ xelatex 编译 → PDF（失败自动修复，最多重试 3 次，仍失败报错给用户）
⑦ 交付：PDF + .tex 保留 + 源码可查看
```

**断网降级**：离线时公式用素材库兜底，Agent 降级为"纯模板替换"（计算/绘图/填表照常）。

---

## 七、关键设计原则

1. **数值真实性**：所有计算列由 Python 代码精确计算，Agent 只做文字，严禁编造
2. **公式由 AI 写**：模板不预置公式，Agent 依据素材库的公式事实动态生成 LaTeX（离线用素材库兜底）
3. **模板最大自主权**：正文（原理/步骤）100% 来自模板，Agent 只做"细微调整"
4. **图表确定性**：图表规格由 chart.json 声明，Agent 不自由发挥
5. **编译兜底**：失败 → Agent 读 .log 自动修复（≤3 次）→ 仍失败给出错误位置和修复建议
6. **个人信息**：姓名/学号/学校等放全局设置，不进模板、不进表单
7. **模型独立配置**：本应用独立一套 LLM 配置（不干扰数模工具）

---

## 八、数据体系

| 数据 | 位置 | 说明 |
|---|---|---|
| 模板库 | `templates/` | 用户导入 |
| 素材库 | `assets/` | 21 个实验的公式/思考题/图表规格 |
| 生成产物 | `outputs/` | PDF + .tex + figures |
| 设置 | `settings.json` | 个人信息、模型、编译、OCR |
| 历史报告 | `outputs/history/` | 可回看 |

全部本地存储，无云端上传。

---

## 九、待定事项 / 路线图

### 待定
- [ ] OCR 引擎已定 RapidOCR（等待落地集成）
- [ ] 模板导入的具体格式细节（用户后续明确）
- [ ] 首批内置素材库范围（21 个全做 or 先做 3-5 个）

### 开发路线
1. 建项目骨架（复制 windows-app-maker 结构）
2. 接入 物理实验.zip 的 common/ 库
3. 模板解析器（三文件 → 表单）
4. 计算引擎 + 绘图模块
5. Agent 组稿（公式编写 + 微调）
6. OCR 集成（RapidOCR）
7. xelatex 编译 + 自动修复
8. 界面（模板库 / 表单 / 预览 / 设置）

---

## 十、参考来源

- RapidOCR 官方仓库：https://github.com/RapidAI/RapidOCR
- PaddleOCR 文档：http://www.paddleocr.ai/main/index.html
- 本地 OCR 方案对比（CSDN 2026-09）：https://blog.csdn.net/zs1028/article/details/160260131
- Python OCR 库实测（CodeSOTA 2026-03）：https://www.codesota.com/ocr/best-for-python
- 物理实验.zip（西安电子科技大学物理实验课程工具，MIT）
